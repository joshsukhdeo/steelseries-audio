# Task Completion

- There is no automated test suite.
- Validate Python syntax manually (e.g. `python -m py_compile steelseries-audio`) after edits.
- Validate Bash syntax manually (`bash -n limusic-watcher.sh`).