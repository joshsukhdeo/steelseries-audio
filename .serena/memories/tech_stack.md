# Tech Stack

- Python 3 for main CLI (`steelseries-audio`), mostly standard library.
- Bash for background daemon (`limusic-watcher.sh`).
- Audio framework: PipeWire, WirePlumber, BlueZ, ALSA.
- System Dependencies: `wpctl`, `pw-dump`, `pactl`, `bluetoothctl`, `speaker-test`, `playerctl`.